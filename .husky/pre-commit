#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Lint-staged runs all linters one by one on staged files that fix and format
# all the code. After all these checks and formatting, husky creates the
# commit on git.
npx lint-staged
